# v9.7.138 Research Start

This tree starts from `v9.7.137-safety` and applies the next-ten research starter stack.

Initial modules added:

- `FIGURE_RENDERER_V2`
- `EFLS_REWIRE`
- `COMPARATOR_ANTISMASH_INGEST`
- `DIRECTED_STUDY_WORKBOOK`
- `LCMS_HANDLE_REGISTRY_V2`
- `CITATION_WORKORDER_RESOLVER`
- `MODEB_FULL20_VALIDATOR`
- `BACKGROUND_CONTROL_BRANCH`
- `DUAL_LLM_HANDOFF_RECEIPT`
- `LEGACY_FEATURE_MATRIX_GATE`

Scope: research starter branch, not public release.

## Downstream (v9.7.338 deliverable surfaces)

The research stack above feeds the current **post-seal, non-scoring / advisory** deliverable menu
(all read already-sealed package outputs; none move AB/AF/novelty priors or the lead tier — class-level
capacity reads only, judgment deferred):

- `good-guesses` (Good Guesses interpretive-priors md/csv/docx/pdf), `modeb-export` (§1–§30 card → docx/pdf).
- `figures kcb-locusmap` (offline KCB comparative locus map), `af-dossier` (Antifungal Lead Dossier).
- `cohort-leads` + `cohort-assemble` (cross-strain priority-leads ledger + cohort assembler),
  `comparator-coverage` (two-denominator comparator-coverage evidence).
- `domain-reference`, `realistic-count`, `novelty-shortlist` (domain dictionary, honest BGC count, novelty shortlist).
- `signoff` (analysis sign-off QC gate) and `verify-modeb --interp` (Mode-B interpretation gate).
