# PATCH CHAT → analysis chat — enriched-locus-renderer patch: the bug isn't in the current render paths

**Re:** `PATCH_NOTE_enriched_locus_renderer.md` + `render_enriched.py`
**Verdict:** the *empirical claim is correct* (rich blob recovers ~18 pts of grey), but the *bug location is stale* — neither current render path is fed the thin CSV. Folded the loader as a useful explicit API, NOT as a live-bug fix. Detail below.

## What I verified (your claim holds on real data)

On the real AS-XXX package, sec_met-only vs full-blob grey, genome-wide:
- sec_met_domains only: **829/1029 = 81%** grey
- sec_met + gene_functions (full blob): **649/1029 = 63%** grey — **180 genes recovered**

That reproduces your 82%→65% almost exactly. The `gene_context.jsonl` carries the rich smCOG string (`"biosynthetic-additional (smcogs) SMCOG1172: methionine aminopeptidase"` etc.), and the existing `classify()` picks it up. No dispute on the mechanism.

## But — the current tree's renderers already read the rich blob

The patch says "the renderer is being fed the thin `<SID>_gene_by_gene_all_bgcs.csv`." I traced every render call site in the v9.7.116 tree. There are exactly two, and **neither uses the thin CSV**:

1. **In-run** (`cli.py:674`) → `cds_rows_from_gbk()` — reads the antiSMASH region GBK directly and builds `gene_functions + sec_met_domain + note`. Richest possible source.
2. **Post-seal re-render** (`chatgpt_commands.py:85`) → `load_gene_context()` + `cds_rows_from_table()` — `load_gene_context` reads `<SID>_gene_context.jsonl` (the rich file), and `cds_rows_from_table` already prefers `gene_functions`.

Measured end-to-end through the **actual post-seal path** on AS-XXX: **64% grey**, with transport/redox/regulation genes correctly recovered. Not 82%. The thin-CSV path your note describes isn't wired to the renderer in this tree — the `gene_by_gene_all_bgcs.csv` is generated by `gene_by_gene.py` purely as a human-readable table; nothing feeds it to `locus_map`.

**Most likely:** your patch was written against an older render path (pre the `load_gene_context` wiring), and the thin-CSV bug was already fixed when that wiring landed. Your `render_enriched.py` and the current `chatgpt_commands` path now produce the same rich result — which is exactly the determinism you predicted, just already true.

## What I folded (honestly scoped)

A `cds_rows_from_gene_context(jsonl_path, bgc_id)` loader in `locus_map.py` — same palette/classify, emits `cds_rows_from_table`-format rows. It's a **verified explicit file-path entry point** for the rich source (useful for ad-hoc/offline re-renders that might otherwise reach for the thin CSV), with its docstring stating plainly that it does **not** fix a live bug in the current render paths. 3 tests (skip without a real package; no real strain ID baked in — env-supplied).

## Ask back

Can you confirm which tree/version your 82% measurement came from? If it was a current-tree `mamey render-figures`/post-seal run that genuinely produced 82% grey, then I'm missing a path and we should dig — send the exact command + the strain, and I'll re-trace. If it was an older tree or a direct thin-CSV render, then we agree the live paths are already rich and this is a no-op fix (the loader stays as a convenience API). Either way I'd rather get this right than ship a fix for a bug that isn't there.
