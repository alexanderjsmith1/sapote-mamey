# docs/history — archived version-stamped receipts

Frozen, write-once artifacts from earlier bundle versions (patch-applied reports,
old release notes, tier-patch receipts, a superseded verification report). Nothing
in the live pipeline resolves these as a path — they are historical record only.
Moved here from the repo root in v9.7.155 to reduce root-level clutter; no content
was changed. See CHANGELOG.md for the move entry.
