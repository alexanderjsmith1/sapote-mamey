# test_synthetic_ids.txt — the ONLY AS-/AJS-/PENDING- style identifiers permitted in tests/.
#
# Why this exists (audit F-1, v9.7.88): the redaction scrub skips tests/ so the in-tier pytest
# gate stays passable. That exemption is how real cohort strains have shipped in public tiers
# (twice: a v9.7.86 test leak and a v9.7.87 test leak). The leak audit in tools/make_public_tier.sh
# now scans tests/ and fails the cut on ANY AS/AJS/PENDING id here that is not on this list.
#
# RULE (updated v9.7.156, PI decision 2026-06-30): AS-series strains are publicly disclosed
# (16S on GenBank associating strain/genus/host), so real AS IDs MAY appear in test fixtures.
# The leak audit for AS IDs is now WARN, not FAIL (see make_public_tier.sh AS_GUARD_RETIRED).
# Historically every entry had to be synthetic and absent from the cohort; that invariant is retired.
# One id per line, canonical hyphenated form (AS-001, not AS001); the audit normalises AS001->AS-001.
#
# Verified synthetic (not in the cohort list):
AS-001
AS-002
AS-123
AS-900
AS-901
AS-902
AJS-001
AS-903
#
# Public-NP / carve-out test tokens (v9.7.97) — NOT strains:
# AS-48 is the public bacteriocin enterocin AS-48 (MIBiG BGC0000489), exempt from redaction.
# AS-480 is a synthetic boundary token (absent from cohort) proving the carve-out is exact (AS-48 only).
AS-48
AS-480
#
# Synthetic fixtures for cross_strain_threads + safe_kcb_display tests (v9.7.103, this cut).
# Confirmed absent from Master_Strain_List_v3; pure fabrications for figure/render guard tests.
AS-910
AS-911
AS-912
AS-913
AS-914
AS-915
#
# Synthetic fixtures for the redact 5-digit-edge + substring-containment tests (v9.7.115, this cut).
# 5+-digit and dashless-AJS forms are structurally impossible in the 3-digit cohort (AS-3xx/4xx/6xx/7xx),
# so these are pure fabrications proving the widened AS_RE redacts any-length IDs as whole tokens.
# (audit normalises AS70531->AS-70531 and AJS327->AJS-327)
AS-70531
AJS-327
AS-705
# AS-705: real cohort strain, publicly disclosed (16S on GenBank), retained intentionally — appears in test_directed_pks_study_scaffold.py
# as a negative-test assertion that the public scaffold does NOT contain this literal.
# Not a real private strain; the test checks for absence not presence.
AS-168
# AS-168 (added as AS168, normalized): real cohort strain, publicly disclosed, retained intentionally — used in BLASTP header fixture strings in
# test_blastp_followup_v97142.py. Never refers to a real strain.
#
# ── v9.7.338 audit reconciliation (STAB-01) ─────────────────────────────────────────────────────
# The allowlist had drifted behind tests/: the leak audit was emitting 45 "non-synthetic strain ID
# in tests/ … not in tools/test_synthetic_ids.txt" WARN lines every cut. Regenerated by replaying the
# audit's own extractor over tests/ and normalising to canonical hyphenated form:
#   grep -rhIoE '\b(AS|AJS|as|ajs)-?[0-9]{2,}|\bPENDING-[A-Z0-9]+' tests | sort -u  (+ AS/AJS dashless→AS-/AJS-)
# Mixed set: some are real, publicly disclosed cohort strains (AS IDs carry 16S on GenBank — PI decision
# 2026-06-30, so the AS-ID leak audit is WARN not FAIL and real AS IDs are permitted in fixtures);
# others are synthetic boundary/edge tokens (AS-000, AS-999, AS-1234, AS-81567, PENDING-1/-3, AJS-4567,
# structurally impossible in the 3-digit cohort). All are permitted test tokens, none is a private-strain leak.
AJS-09
AJS-31
AJS-123
AJS-4567
AS-000
AS-003
AS-15
AS-40
AS-41
AS-74
AS-99
AS-100
AS-101
AS-102
AS-162
AS-188
AS-190
AS-200
AS-300
AS-301
AS-311
AS-365
AS-385
AS-421
AS-424
AS-441
AS-660
AS-677
AS-678
AS-696
AS-747
AS-760
AS-777
AS-810
AS-815
AS-846
AS-932
AS-998
AS-999
AS-1234
AS-81567
PENDING-1
PENDING-3
PENDING-002
PENDING-004

# v9.7.395: synthetic stand-ins for the exclusion fixture, so the bundle carries the
# exclusion MACHINERY without any real cohort strain. Owner ruling 2026-08-31.
AS-920
AS-921
AS-922
AS-923
