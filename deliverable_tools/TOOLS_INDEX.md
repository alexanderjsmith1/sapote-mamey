# TOOLS_INDEX — Sapote-Mamey Multi-Cohort Comparison Toolkit

Auto-generated by `cohort_tools.py index`. 22 tools.

## BLASTp / channel ingest

| tool | what it does |
|---|---|
| `backfill_coords_from_antismash` | for strains whose roster_v2.json lacks gene |
| `enrich_clusterblast` | add the general antiSMASH ClusterBlast channel to a |
| `enrich_swissprot` | fill the swissprot channel in roster_v2.json from the |
| `extract_cohort_clusterblast` | parse per-gene KnownClusterBlast (MIBiG) from the |
| `extract_comprehensive_both` | (no docstring — run with -h) |
| `ingest_blastp` | (no docstring — run with -h) |

## Cohort analysis (AS vs SID, novelty)

| tool | what it does |
|---|---|
| `build_novelty_board` | cross-strain novelty board: each strain's top v3 leads, |
| `fair_cohort_analysis` | AS vs SID compound comparison + confounder analysis, both |

## Interactive widgets

| tool | what it does |
|---|---|
| `bgc_widget` | (no docstring — run with -h) |

## Other

| tool | what it does |
|---|---|
| `_batch_enrich.sh` | (no docstring — run with -h) |
| `_build_all.sh` | (no docstring — run with -h) |
| `rebuild_cohort_comparison` | 25 SID from the gene master + 64 SID from the extracted supplement = 89 SID vs 46 AS. |

## Phylogenomics & genome set

| tool | what it does |
|---|---|
| `build_working_genome_set` | one clean, content-deduplicated genome pool + provenance. |
| `insert_tree_into_report` | append the phylogenomic tree to a strain's Word report. |
| `render_mlsa_tree` | Drop leaves whose name matches any substring, collapsing now-unifurcating parents. |

## Rosters & dossiers

| tool | what it does |
|---|---|
| `_build_v3_all.sh` | (no docstring — run with -h) |
| `build_cohort_index` | cohort index (md + html landing) over all roster_v2 JSONs. |
| `build_strain_dossier` | (no docstring — run with -h) |
| `build_v1` | Rebuild the v1 BGC protein roster (best-NCBI-nr-hit-per-gene, grouped by BGC |
| `build_v2_md` | detailed v2 markdown (+ optional docx) from roster_v2.json, |
| `build_v3` | v3 BGC protein roster: per-BGC synthesis headers (with denominators), |
| `roster_v2` | Pick the canonical *_2_inventory.csv for a strain (prefer sealed mamey_packages). |
